/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'cdn.lynkid.my.id',
      'cdn.sanity.io',
      'rhinoindonesia.com'
    ],
  },
};

export default nextConfig;
